## Description

This PR completes the Developer Portal settings requirements by introducing the mock webhook test delivery system.

* **Webhook Controller**: Added `testWebhook` function to load the requested endpoint and enqueue a mock `TEST_EVENT` delivery payload.
* **Webhook Routes**: Registered the endpoint under `POST /api/webhooks/:id/test` with rate limiting and `MANAGE_WEBHOOKS` permission gates.

---

## Related Issue

* Closes #1387

---

## Component(s) Affected

* [x] Backend (backend/)
* [ ] Mobile app
* [ ] Web app
* [ ] Docs only
* [ ] CI / tooling

---

## Type of Change

* [x] New feature
* [ ] Bug fix
* [ ] Refactor
* [ ] Other:

---

## Testing Performed

* Enqueued mock test payloads successfully.
* Verified that non-existent webhook IDs return `404 Not Found`.
* Verified rate limiting.

---

## Checklist

* [x] Rebased from latest main - zero merge conflicts
* [x] Clean and simple code with JSDocs comments
* [x] No secrets committed